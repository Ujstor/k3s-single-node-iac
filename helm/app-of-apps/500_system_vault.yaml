apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: system-vault
  namespace: gitops
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: vault
    server: https://kubernetes.default.svc
  project: default
  source:
    path: helm/system/vault
    repoURL: https://github.com/Ujstor/k3s-single-node-iac
    targetRevision: master
    helm:
      releaseName: vault
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=true
